if WeaponFactoryManager and tweak_data and tweak_data.statistics then
	local _, _, _, weapon_list, _, _, _, _, _ = tweak_data.statistics:statistics_table()
	for _, weapon_id in pairs(weapon_list) do
	
	end
end